/**
* Este archivo contiene la URL de la API de la aplicación.
* Se utiliza para realizar solicitudes al servidor backend.
* La URL se puede modificar según el entorno (desarrollo, producción, etc.).
* Actualmente, está configurada en un servidor local que se ejecuta en el puerto 8000.
*/
const API_URL = 'http://localhost:8000/api';

export default API_URL;